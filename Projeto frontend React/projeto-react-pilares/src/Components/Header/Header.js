import {HeaderContainer} from "./Style"

const Header = () => {
    return ( <HeaderContainer>
        <header>
        <h1>Store</h1>
      </header>
      </HeaderContainer>
);
}
 
export default Header;